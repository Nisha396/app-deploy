name: deploy-app
on:
  pull_request:
    branches: 
      - main
    types:
      - opened
      - synchronize
      - reopened
  push:
    branches:
      - main
jobs:
  Deploy:
    runs-on: ubuntu:latest
    steps:
     - name: deploy
       run: echo "Deploying app..."
